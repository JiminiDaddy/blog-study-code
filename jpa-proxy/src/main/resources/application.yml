spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  datasource:
    url: jdbc:h2:mem:testdb;
    username: sa
    password:
    driverClassName: org.h2.Driver

logging:
  level:
    org:
      hibernate:
        SQL: debug
        type: trace

